'use strict'

let { isClean, my } = require('./symbols')
let Declaration = require('./declaration')
let Comment = require('./comment')
let Node = require('./node')

let parse, Rule, AtRule, Root

function cleanSource(nodes) {
  return nodes.map(i => {
    if (i.nodes) i.nodes = cleanSource(i.nodes)
    delete i.source
    return i
  })
}

function markDirtyUp(node) {
  node[isClean] = false
  if (node.proxyOf.nodes) {
    for (let i of node.proxyOf.nodes) {
      markDirtyUp(i)
    }
  }
}

class Container extends Node {
  append(...children) {
    for (let child of children) {
      let nodes = this.normalize(child, this.last)
      for (let node of nodes) this.proxyOf.nodes.push(node)
    }

    this.markDirty()

    return this
  }

  cleanRaws(keepBetween) {
    super.cleanRaws(keepBetween)
    if (this.nodes) {
      for (let node of this.nodes) node.cleanRaws(keepBetween)
    }
  }

  each(callback) {
    if (!this.proxyOf.nodes) return undefined
    let iterator = this.getIterator()

    let index, result
    while (this.indexes[iterator] < this.proxyOf.nodes.length) {
      index = this.indexes[iterator]
      result = callback(this.proxyOf.nodes[index], index)
      if (result === false) break

      this.indexes[iterator] += 1
    }

    delete this.indexes[iterator]
    return result
  }

  every(condition) {
    return this.nodes.every(condition)
  }

  getIterator() {
    if (!this.lastEach) this.lastEach = 0
    if (!this.indexes) this.indexes = {}

    this.lastEach += 1
    let iterator = this.lastEach
    this.indexes[iterator] = 0

    return iterator
  }

  getProxyProcessor() {
    return {
      get(node, prop) {
        if (prop === 'proxyOf') {
          return node
        } else if (!node[prop]) {
          return node[prop]
        } else if (
          prop === 'each' ||
          (typeof prop === 'string' && prop.startsWith('walk'))
        ) {
          return (...args) => {
            return node[prop](
              ...args.map(i => {
                if (typeof i === 'function') {
                  return (child, index) => i(child.toProxy(), index)
                } else {
                  return i
                }
              })
            )
          }
        } else if (prop === 'every' || prop === 'some') {
          return cb => {
            return node[prop]((child, ...other) =>
              cb(child.toProxy(), ...other)
            )
          }
        } else if (prop === 'root') {
          return () => node.root().toProxy()
        } else if (prop === 'nodes') {
          return node.nodes.map(i => i.toProxy())
        } else if (prop === 'first' || prop === 'last') {
          return node[prop].toProxy()
        } else {
          return node[prop]
        }
      },

      set(node, prop, value) {
        if (node[prop] === value) return true
        node[prop] = value
        if (prop === 'name' || prop === 'params' || prop === 'selector') {
          node.markDirty()
        }
        return true
      }
    }
  }

  index(child) {
    if (typeof child === 'number') return child
    if (child.proxyOf) child = child.proxyOf
    return this.proxyOf.nodes.indexOf(child)
  }

  insertAfter(exist, add) {
    let existIndex = this.index(exist)
    let nodes = this.normalize(add, this.proxyOf.nodes[existIndex]).reverse()
    existIndex = this.index(exist)
    for (let node of nodes) this.proxyOf.nodes.splice(existIndex + 1, 0, node)

    let index
    for (let id in this.indexes) {
      index = this.indexes[id]
      if (existIndex < index) {
        this.indexes[id] = index + nodes.length
      }
    }

    this.markDirty()

    return this
  }

  insertBefore(exist, add) {
    let existIndex = this.index(exist)
    let type = existIndex === 0 ? 'prepend' : false
    let nodes = this.normalize(add, this.proxyOf.nodes[existIndex], type).reverse()
    existIndex = this.index(exist)
    for (let node of nodes) this.proxyOf.nodes.splice(existIndex, 0, node)

    let index
    for (let id in this.indexes) {
      index = this.indexes[id]
      if (existIndex <= index) {
        this.indexes[id] = index + nodes.length
      }
    }

    this.markDirty()

    return this
  }

  normalize(nodes, sample) {
    if (typeof nodes === 'string') {
      nodes = cleanSource(parse(nodes).nodes)
    } else if (typeof nodes === 'undefined') {
      nodes = []
    } else if (Array.isArray(nodes)) {
      nodes = nodes.slice(0)
      for (let i of nodes) {
        if (i.parent) i.parent.removeChild(i, 'ignore')
      }
    } else if (nodes.type === 'root' && this.type !== 'document') {
      nodes = nodes.nodes.slice(0)
      for (let i of nodes) {
        if (i.parent) i.parent.removeChild(i, 'ignore')
      }
    } else if (nodes.type) {
      nodes = [nodes]
    } else if (nodes.prop) {
      if (typeof nodes.value === 'undefined') {
        throw new Error('Value field is missed in node creation')
      } else if (typeof nodes.value !== 'string') {
        nodes.value = String(nodes.value)
      }
      nodes = [new Declaration(nodes)]
    } else if (nodes.selector) {
      nodes = [new Rule(nodes)]
    } else if (nodes.name) {
      nodes = [new AtRule(nodes)]
    } else if (nodes.text) {
      nodes = [new Comment(nodes)]
    } else {
      throw new Error('Unknown node type in node creation')
    }

    let processed = nodes.map(i => {
      /* c8 ignore next */
      if (!i[my]) Container.rebuild(i)
      i = i.proxyOf
      if (i.parent) i.parent.removeChild(i)
      if (i[isClean]) markDirtyUp(i)
      if (typeof i.raws.before === 'undefined') {
        if (sample && typeof sample.raws.before !== 'undefined') {
          i.raws.before = sample.raws.before.replace(/\S/g, '')
        }
      }
      i.parent = this.proxyOf
      return i
    })

    return processed
  }

  prepend(...children) {
    children = children.reverse()
    for (let child of children) {
      let nodes = this.normalize(child, this.first, 'prepend').reverse()
      for (let node of nodes) this.proxyOf.nodes.unshift(node)
      for (let id in this.indexes) {
        this.indexes[id] = this.indexes[id] + nodes.length
      }
    }

    this.markDirty()

    return this
  }

  push(child) {
    child.parent = this
    this.proxyOf.nodes.push(child)
    return this
  }

  removeAll() {
    for (let node of this.proxyOf.nodes) node.parent = undefined
    this.proxyOf.nodes = []

    this.markDirty()

    return this
  }

  removeChild(child) {
    child = this.index(child)
    this.proxyOf.nodes[child].parent = undefined
    this.proxyOf.nodes.splice(child, 1)

    let index
    for (let id in this.indexes) {
      index = this.indexes[id]
      if (index >= child) {
        this.indexes[id] = index - 1
      }
    }

    this.markDirty()

    return this
  }

  replaceValues(pattern, opts, callback) {
    if (!callback) {
      callback = opts
      opts = {}
    }

    this.walkDecls(decl => {
      if (opts.props && !opts.props.includes(decl.prop)) return
      if (opts.fast && !decl.value.includes(opts.fast)) return

      decl.value = decl.value.replace(pattern, callback)
    })

    this.markDirty()

    return this
  }

  some(condition) {
    return this.nodes.some(condition)
  }

  walk(callback) {
    return this.each((child, i) => {
      let result
      try {
        result = callback(child, i)
      } catch (e) {
        throw child.addToError(e)
      }
      if (result !== false && child.walk) {
        result = child.walk(callback)
      }

      return result
    })
  }

  walkAtRules(name, callback) {
    if (!callback) {
      callback = name
      return this.walk((child, i) => {
        if (child.type === 'atrule') {
          return callback(child, i)
        }
      })
    }
    if (name instanceof RegExp) {
      return this.walk((child, i) => {
        if (child.type === 'atrule' && name.test(child.name)) {
          return callback(child, i)
        }
      })
    }
    return this.walk((child, i) => {
      if (child.type === 'atrule' && child.name === name) {
        return callback(child, i)
      }
    })
  }

  walkComments(callback) {
    return this.walk((child, i) => {
      if (child.type === 'coÌ|Ö ¡Ë€X/vÕoö•‘t<Í       H     @8         Òl¶P∫ı∏QQ›*[%Üì’ÔAU5x"µ√anï       J     @8         èÚ⁄ æFfú8V_Ã¨∑“¡∆kŸÕØÔ√ä˝ŸñO:       K     @8         ¥1o.8“ÍôcΩ›Íßπº˝©∂‹ﬁÇñ¸π2~…Ìı       M     @8         ™ÙÚÎMﬁ®séÈ>∫∏M}ÜåÛz[˙tÆkëõnàúD<x       P     @8         O ∞y–r¢fË√l-äÃq"É (ègÍŒÃT=       Q     @8         Á≥eïU@‚´Õ}y%ná˚?∫Ãj:≠ü€ö]Ωÿæ•       S     @8         z˙-ò‚/Ã6y`F¸_£Í&©)√|@ù“#W˛æ{'       `     @8         	ù9∞eH¥∫Ä”^(˜ÿ⁄…Ÿf/‘B≤'Ë‰›ÄÚ¥V       a     @8         @A≈ÅøÔ^%\K˜¸©<Œ]ëVÚ•_Ù˚‡ß8Æw¯wÇ       b     @8         é˝~Ó\¡U⁄¨›¸(◊áD§ˆf†!å8áÆ¢Ñü5:$       j     @8         ‹x·2‹ït3(Bÿˆô‡¿"◊ÙÂ/‘H)O∞¡RÛí       o     @8         ˆPí(VﬁGÀ…]p5@ŒÒ«zªªhÔ4û6±™       q     @8         ºÏQ¢∆Y'∑3çoåRî˘∆Ê^Qò]M\ŒKÌKö¥       x     @8         %ı˛®˚ù≠úP”as˘ÏÎs5”àì’sSSõ_LΩ       z     @8         s@„$¥ Õ\RûæTê∫rœ∑}ÊÎ`˜çQr⁄eùíä       {     @8         $úYró,Ù¥≈-Óì*ˆs€PˇÔ∏¨†∆JÎË$2ÎÕÏg       |     @8         ˚'l¯É~ZT%ëù=÷V•ƒÅ_èFMöÕ¡Ùë       ~     @8         \Ÿå›∫ú§NäxÆò‡
Ö_{~Xo¨*˚úˇÛ2@4(       @     @8         ïX,~ªå’¥⁄ë·Ï	Œå ©«ÆD1ıáŒ>€ë3l6       C     @8         ≥‰öÌ~n7Ê‹Y&ãªHœ5%Úûä®SæÚyfû       E     @8         ·ú´míâ–q˛k_ Õ.∞ÅÕ[¬∆-c£@rÕÅ7œ       I     @8         Ùe¢D>∫…†≠⁄Fãn∑™±s•\âÛôÏ7	vkéÈœû       T     @8         ∫åhÅ¡JÆãÄ≈lik_$!˝;ìT©◊rB       V     @8         1
TË/~°&EN’{‚`‰ﬂ®‹ª.™”!‘       ]     @,        ^aT¶‚ñ|x7]¿,ﬂ⁄ 9sõyD       _     @8         xÁ0=ˇõû°p˙õâ6áΩ'A›¥|áãóX}cdo       g     @8         7LêΩŸé¥ÑñÎ}ÚŸî®í∆S˛˘yT˛ì„`´£       k     @8         Öx˘ŒÀˆèKôö∆z˝_Öc?;?§iœ0˘î…ûZ       w     @8         ﬂ∫JpQÜqõ7ß,v ˘Ô1ÂÏoÊé?W•ª       y     @8         õ`√∂u«wŸëÀw5â>≤=]Iß°◊Í\E[A·=       {     @8         Xo)Û™8◊ä6ˆÅj|§(r∂ñÇEàM!≠M£       Ä     @8         ¨ıZΩFi1i·¯D®Mñö@Ÿd¨πz∫ÙïB3	ÔP       ä     @8         ∫)"B°LcÄ›NÖbÊπv•k≠g‰ı5j⁄ÉU       ∏     @8         cS"É;ü<ûâüc≈ht,.ç 2éÕ(jÛåcl       π     @8         úesª÷NA§˙L=éQkr˝)x”2;·LÛ‰a¥       –     @8         ‡RŸ+UûmæÀ?‡¸°áå3'`}[´Ì≠5?       ⁄     @8         #p0˘≠ÇË@w+·¬±›üuπÓm™R*z∏õ-e